{# 
Template for generating .pyi stub files for endpoint classes
Focus: Payload TypedDict and Enum Literals for IDE autocomplete
#}
from typing import TypedDict, Literal, NotRequired, Any, Coroutine, Union

{% if schema.fields %}
# Payload TypedDict for IDE autocomplete
class {{ schema.class_name }}Payload(TypedDict, total=False):
    """
    Type hints for {{ schema.path }} payload fields.
    
    Use this for IDE autocomplete when building payload dicts:
        payload: {{ schema.class_name }}Payload = {
            "field": "value",  # <- autocomplete shows all fields
        }
    """
{% for field_name, field in schema.fields.items() %}
{% set field_snake = field_name|kebab_to_snake %}
{% if field_snake not in ['class', 'def', 'import', 'from', 'global', 'return', 'if', 'elif', 'else', 'for', 'while', 'with', 'try', 'except', 'finally', 'raise', 'assert', 'pass', 'break', 'continue', 'lambda', 'yield'] %}
{# Check if field has enum options #}
{% if field.options %}
    {{ field_snake }}: {% if not field.required %}NotRequired[{% endif %}Literal[{{ field.options|map('tojson')|join(', ') }}]{% if not field.required %}]{% endif %}  # {{ field.help[:60] if field.help else field_name }}
{% elif field.type == 'integer' %}
    {{ field_snake }}: {% if not field.required %}NotRequired[{% endif %}int{% if not field.required %}]{% endif %}  # {{ field.help[:60] if field.help else field_name }}
{% elif field.type == 'option' and field.enum_values %}
    {{ field_snake }}: {% if not field.required %}NotRequired[{% endif %}Literal[{{ field.enum_values|map('tojson')|join(', ') }}]{% if not field.required %}]{% endif %}  # {{ field.help[:60] if field.help else field_name }}
{% elif field.is_list %}
    {{ field_snake }}: {% if not field.required %}NotRequired[{% endif %}list[dict[str, Any]]{% if not field.required %}]{% endif %}  # {{ field.help[:60] if field.help else field_name }}
{% else %}
    {{ field_snake }}: {% if not field.required %}NotRequired[{% endif %}str{% if not field.required %}]{% endif %}  # {{ field.help[:60] if field.help else field_name }}
{% endif %}
{% endif %}
{% endfor %}

{% endif %}

class {{ schema.class_name }}:
    """
    {{ schema.help if schema.help else schema.path + ' endpoint' }}
    
    Path: {{ schema.path }}
    Category: {{ schema.category }}
{% if schema.mkey %}
    Primary Key: {{ schema.mkey }}
{% endif %}
    """
    
    def get(
        self,
{% if schema.mkey %}
        {{ schema.mkey|kebab_to_snake }}: {{ schema.mkey_type|to_python_type }} | None = ...,
{% else %}
        name: str | None = ...,
{% endif %}
        filter: str | None = ...,
        range: list[int] | None = ...,
        sort: str | None = ...,
        format: str | None = ...,
        action: str | None = ...,
        vdom: str | bool | None = ...,
        raw_json: bool = ...,
        **kwargs: Any,
    ) -> Union[dict[str, Any], Coroutine[Any, Any, dict[str, Any]]]: ...
    
    def post(
        self,
{% if schema.fields %}
        payload_dict: {{ schema.class_name }}Payload | None = ...,
{% else %}
        payload_dict: dict[str, Any] | None = ...,
{% endif %}
{% for field_name, field in schema.fields.items() %}
{% set field_snake = field_name|kebab_to_snake %}
{% if field_snake not in ['vdom', 'raw_json', 'kwargs', 'class', 'def', 'import', 'from', 'global', 'return', 'if', 'elif', 'else', 'for', 'while', 'with', 'try', 'except', 'finally', 'raise', 'assert', 'pass', 'break', 'continue', 'lambda', 'yield'] %}
{# Show enum Literal types for better autocomplete #}
{% if field.options %}
        {{ field_snake }}: Literal[{{ field.options|map('tojson')|join(', ') }}] | None = ...,
{% elif field.type == 'integer' %}
        {{ field_snake }}: int | None = ...,
{% elif field.type == 'option' and field.enum_values %}
        {{ field_snake }}: Literal[{{ field.enum_values|map('tojson')|join(', ') }}] | None = ...,
{% elif field.is_list %}
        {{ field_snake }}: list[dict[str, Any]] | None = ...,
{% else %}
        {{ field_snake }}: str | None = ...,
{% endif %}
{% endif %}
{% endfor %}
        vdom: str | bool | None = ...,
        raw_json: bool = ...,
        **kwargs: Any,
    ) -> Union[dict[str, Any], Coroutine[Any, Any, dict[str, Any]]]: ...
    
    def put(
        self,
{% if schema.fields %}
        payload_dict: {{ schema.class_name }}Payload | None = ...,
{% else %}
        payload_dict: dict[str, Any] | None = ...,
{% endif %}
{% for field_name, field in schema.fields.items() %}
{% set field_snake = field_name|kebab_to_snake %}
{% if field_snake not in ['vdom', 'raw_json', 'kwargs', 'class', 'def', 'import', 'from', 'global', 'return', 'if', 'elif', 'else', 'for', 'while', 'with', 'try', 'except', 'finally', 'raise', 'assert', 'pass', 'break', 'continue', 'lambda', 'yield'] %}
{% if field.options %}
        {{ field_snake }}: Literal[{{ field.options|map('tojson')|join(', ') }}] | None = ...,
{% elif field.type == 'integer' %}
        {{ field_snake }}: int | None = ...,
{% elif field.type == 'option' and field.enum_values %}
        {{ field_snake }}: Literal[{{ field.enum_values|map('tojson')|join(', ') }}] | None = ...,
{% elif field.is_list %}
        {{ field_snake }}: list[dict[str, Any]] | None = ...,
{% else %}
        {{ field_snake }}: str | None = ...,
{% endif %}
{% endif %}
{% endfor %}
        vdom: str | bool | None = ...,
        raw_json: bool = ...,
        **kwargs: Any,
    ) -> Union[dict[str, Any], Coroutine[Any, Any, dict[str, Any]]]: ...
    
    def delete(
        self,
{% if schema.mkey %}
        {{ schema.mkey|kebab_to_snake }}: {{ schema.mkey_type|to_python_type }} | None = ...,
{% else %}
        name: str | None = ...,
{% endif %}
        vdom: str | bool | None = ...,
        raw_json: bool = ...,
        **kwargs: Any,
    ) -> Union[dict[str, Any], Coroutine[Any, Any, dict[str, Any]]]: ...
    
    def exists(
        self,
{% if schema.mkey %}
        {{ schema.mkey|kebab_to_snake }}: {{ schema.mkey_type|to_python_type }},
{% else %}
        name: str,
{% endif %}
        vdom: str | bool | None = ...,
    ) -> Union[bool, Coroutine[Any, Any, bool]]: ...
    
    def set(
        self,
{% if schema.fields %}
        payload_dict: {{ schema.class_name }}Payload | None = ...,
{% else %}
        payload_dict: dict[str, Any] | None = ...,
{% endif %}
        vdom: str | bool | None = ...,
        **kwargs: Any,
    ) -> Union[dict[str, Any], Coroutine[Any, Any, dict[str, Any]]]: ...
    
    # Helper methods
    @staticmethod
    def help(field_name: str | None = ...) -> str: ...
    
    @staticmethod
    def fields(detailed: bool = ...) -> Union[list[str], list[dict[str, Any]]]: ...
    
    @staticmethod
    def field_info(field_name: str) -> dict[str, Any]: ...
    
    @staticmethod
    def validate_field(name: str, value: Any) -> bool: ...
    
    @staticmethod
    def required_fields() -> list[str]: ...
    
    @staticmethod
    def defaults() -> dict[str, Any]: ...
    
    @staticmethod
    def schema() -> dict[str, Any]: ...


__all__ = [
    "{{ schema.class_name }}",
{% if schema.fields %}
    "{{ schema.class_name }}Payload",
{% endif %}
]
